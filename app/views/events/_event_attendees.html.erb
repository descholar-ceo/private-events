<h3 class="event-title"><%=event.event_name%>'s attendees are:</h3>
  <table>
    <tbody>
      <%event.event_attendee.each do |attendee|%>
        <tr>
          <td><%=attendee.username%></td>
          <td><%=attendee.email%></td>
        </tr>
      <%end%>
    </tbody>
  </table>
